theme(scheme = scheme(#933 #393 #339))
  generate-variables()
  generate-scaffolding()
  generate-grid()
  generate-buttons(scheme)
  generate-forms(scheme)
  generate-tables()
  generate-utilities()
